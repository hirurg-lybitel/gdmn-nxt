import { render } from '@testing-library/react';

import StyledSplit from './styled-split';

describe('StyledSplit', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<StyledSplit />);
    expect(true).toBeTruthy();
  });
});

import { render } from '@testing-library/react';

import BusinessDirectionCompare from './business-direction-compare';

describe('BusinessDirectionCompare', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<BusinessDirectionCompare />);
    expect(true).toBeTruthy();
  });
});

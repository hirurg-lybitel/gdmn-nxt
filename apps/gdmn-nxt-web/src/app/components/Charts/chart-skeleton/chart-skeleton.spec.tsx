import { render } from '@testing-library/react';

import ChartSkeleton from './chart-skeleton';

describe('ChartSkeleton', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<ChartSkeleton />);
    expect(true).toBeTruthy();
  });
});

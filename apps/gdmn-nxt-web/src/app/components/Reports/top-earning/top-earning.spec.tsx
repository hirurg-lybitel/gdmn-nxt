import { render } from '@testing-library/react';

import TopEarning from './top-earning';

describe('TopEarning', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<TopEarning />);
    expect(true).toBeTruthy();
  });
});

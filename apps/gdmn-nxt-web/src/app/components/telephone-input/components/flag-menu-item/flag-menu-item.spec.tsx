import { render } from '@testing-library/react';

import FlagMenuItem from './flag-menu-item';

describe('FlagMenuItem', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<FlagMenuItem />);
    expect(true).toBeTruthy();
  });
});

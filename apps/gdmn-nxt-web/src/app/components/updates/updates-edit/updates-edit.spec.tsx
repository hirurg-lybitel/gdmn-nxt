import { render } from '@testing-library/react';

import UpdatesEdit from './updates-edit';

describe('UpdatesEdit', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<UpdatesEdit />);
    expect(true).toBeTruthy();
  });
});

import { render } from '@testing-library/react';

import UpdatesInfo from './updates-info';

describe('UpdatesInfo', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<UpdatesInfo />);
    expect(true).toBeTruthy();
  });
});

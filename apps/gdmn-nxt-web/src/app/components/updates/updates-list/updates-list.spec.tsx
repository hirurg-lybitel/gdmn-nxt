import { render } from '@testing-library/react';

import UpdatesList from './updates-list';

describe('UpdatesList', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<UpdatesList />);
    expect(true).toBeTruthy();
  });
});

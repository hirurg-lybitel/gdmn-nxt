import { render } from '@testing-library/react';

import ItemButtonEdit from './item-button-edit';

describe('ItemButtonEdit', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<ItemButtonEdit />);
    expect(true).toBeTruthy();
  });
});

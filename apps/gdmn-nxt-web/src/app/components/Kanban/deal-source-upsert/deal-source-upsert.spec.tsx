import { render } from '@testing-library/react';

import DealSourceUpsert from './deal-source-upsert';

describe('DealSourceUpsert', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<DealSourceUpsert />);
    expect(true).toBeTruthy();
  });
});

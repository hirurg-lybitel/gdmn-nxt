import { render } from '@testing-library/react';

import DealsFilter from './deals-filter';

describe('DealsFilter', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<DealsFilter />);
    expect(true).toBeTruthy();
  });
});

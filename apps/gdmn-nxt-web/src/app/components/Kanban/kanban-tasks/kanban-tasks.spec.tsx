import { render } from '@testing-library/react';

import KanbanTasks from './kanban-tasks';

describe('KanbanTasks', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<KanbanTasks />);
    expect(true).toBeTruthy();
  });
});

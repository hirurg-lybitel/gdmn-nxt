import { render } from '@testing-library/react';

import KanbanHistory from './kanban-history';

describe('KanbanHistory', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<KanbanHistory />);
    expect(true).toBeTruthy();
  });
});

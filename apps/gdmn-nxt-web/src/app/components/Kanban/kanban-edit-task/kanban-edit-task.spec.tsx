import { render } from '@testing-library/react';

import KanbanEditTask from './kanban-edit-task';

describe('KanbanEditTask', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<KanbanEditTask />);
    expect(true).toBeTruthy();
  });
});

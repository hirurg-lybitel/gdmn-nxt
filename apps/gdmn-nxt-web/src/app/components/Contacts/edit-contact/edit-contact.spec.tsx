import { render } from '@testing-library/react';

import EditContact from './edit-contact';

describe('EditContact', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<EditContact />);
    expect(true).toBeTruthy();
  });
});

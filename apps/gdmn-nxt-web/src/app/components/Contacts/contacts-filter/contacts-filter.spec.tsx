import { render } from '@testing-library/react';

import ContactsFilter from './contacts-filter';

describe('ContactsFilter', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<ContactsFilter />);
    expect(true).toBeTruthy();
  });
});

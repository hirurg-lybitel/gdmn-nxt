import { render } from '@testing-library/react';

import CustomizedScrollBox from './customized-scroll-box';

describe('CustomizedScrollBox', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<CustomizedScrollBox />);
    expect(true).toBeTruthy();
  });
});

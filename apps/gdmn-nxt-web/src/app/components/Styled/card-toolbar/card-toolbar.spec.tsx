import { render } from '@testing-library/react';

import CardToolbar from './card-toolbar';

describe('CardToolbar', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<CardToolbar />);
    expect(true).toBeTruthy();
  });
});

import { render } from '@testing-library/react';

import StyledTreeView from './styled-tree-view';

describe('StyledTreeView', () => {
  it('should render successfully', () => {
    //const { baseElement } = render(<StyledTreeView />);
    expect(true).toBeTruthy();
  });
});

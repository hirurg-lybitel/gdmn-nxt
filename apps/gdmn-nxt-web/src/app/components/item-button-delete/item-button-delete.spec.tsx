import { render } from '@testing-library/react';

import ItemButtonDelete from './item-button-delete';

describe('ItemButtonDelete', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<ItemButtonDelete />);
    expect(true).toBeTruthy();
  });
});

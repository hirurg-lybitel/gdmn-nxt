import { render } from '@testing-library/react';

import TextFieldMasked from './textField-masked';

describe('TextFieldMasked', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<TextFieldMasked />);
    expect(true).toBeTruthy();
  });
});

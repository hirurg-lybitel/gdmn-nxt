import { render } from '@testing-library/react';

import PermissionGate from './permission-gate';

describe('PermissionGate', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<PermissionGate />);
    expect(true).toBeTruthy();
  });
});

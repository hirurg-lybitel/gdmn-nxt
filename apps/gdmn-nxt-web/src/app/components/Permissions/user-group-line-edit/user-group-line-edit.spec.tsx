import { render } from '@testing-library/react';

import UserGroupLineEdit from './user-group-line-edit';

describe('UserGroupLineEdit', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<UserGroupLineEdit />);
    expect(true).toBeTruthy();
  });
});

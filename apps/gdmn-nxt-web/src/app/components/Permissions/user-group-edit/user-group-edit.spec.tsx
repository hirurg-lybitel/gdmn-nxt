import { render } from '@testing-library/react';

import UserGroupEdit from './user-group-edit';

describe('UserGroupEdit', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<UserGroupEdit />);
    expect(true).toBeTruthy();
  });
});

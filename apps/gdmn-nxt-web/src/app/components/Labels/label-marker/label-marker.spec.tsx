import { render } from '@testing-library/react';

import LabelMarker from './label-marker';

describe('LabelMarker', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<LabelMarker />);
    expect(true).toBeTruthy();
  });
});

import { render } from '@testing-library/react';

import LabelListItemEdit from './label-list-item-edit';

describe('LabelListItemEdit', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<LabelListItemEdit />);
    expect(true).toBeTruthy();
  });
});

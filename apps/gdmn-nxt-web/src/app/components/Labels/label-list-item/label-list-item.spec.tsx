import { render } from '@testing-library/react';

import LabelListItem from './label-list-item';

describe('LabelListItem', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<LabelListItem />);
    expect(true).toBeTruthy();
  });
});

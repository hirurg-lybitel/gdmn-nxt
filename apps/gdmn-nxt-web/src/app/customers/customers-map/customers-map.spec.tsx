import { render } from '@testing-library/react';

import CustomersMap from './customers-map';

describe('CustomersMap', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<CustomersMap />);
    expect(true).toBeTruthy();
  });
});

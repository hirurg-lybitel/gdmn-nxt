import { render } from '@testing-library/react';

import ActCompletion from './act-completion';

describe('ActCompletion', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<ActCompletion />);
    expect(true).toBeTruthy();
  });
});

import { render } from '@testing-library/react';

import BankStatement from './bank-statement';

describe('BankStatement', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<BankStatement />);
    expect(true).toBeTruthy();
  });
});
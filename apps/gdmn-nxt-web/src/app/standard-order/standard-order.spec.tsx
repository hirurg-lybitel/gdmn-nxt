import { render } from '@testing-library/react';

import StandardOrder from './standard-order';

describe('StandardOrder', () => {
  it('should render successfully', () => {
    //const { baseElement } = render(<StandardOrder />);
    expect(true).toBeTruthy();
  });
});

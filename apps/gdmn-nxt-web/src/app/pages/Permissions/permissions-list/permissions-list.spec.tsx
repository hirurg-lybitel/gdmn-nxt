import { render } from '@testing-library/react';

import PermissionsList from './permissions-list';

describe('PermissionsList', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<PermissionsList />);
    expect(true).toBeTruthy();
  });
});

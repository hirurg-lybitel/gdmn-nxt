import { render } from '@testing-library/react';

import DealSources from './deal-sources';

describe('DealSources', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<DealSources />);
    expect(true).toBeTruthy();
  });
});

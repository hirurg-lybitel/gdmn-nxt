import { render } from '@testing-library/react';

import DenyReasons from './deny-reasons';

describe('DenyReasons', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<DenyReasons />);
    expect(true).toBeTruthy();
  });
});

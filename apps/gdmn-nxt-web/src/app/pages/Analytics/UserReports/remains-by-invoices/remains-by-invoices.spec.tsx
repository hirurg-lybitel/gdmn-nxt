import { render } from '@testing-library/react';

import RemainsByInvoices from './remains-by-invoices';

describe('RemainsByInvoices', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<RemainsByInvoices />);
    expect(true).toBeTruthy();
  });
});

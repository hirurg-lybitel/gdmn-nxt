import { render } from '@testing-library/react';

import ViewUserNotifications from './view-user-notifications';

describe('ViewUserNotifications', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<ViewUserNotifications />);
    expect(true).toBeTruthy();
  });
});
